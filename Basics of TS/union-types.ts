let userId: number | string | boolean | { name: string };
userId = 1;
userId = "vaishnav";
userId = true;
userId = {name : "vaishnav"};
console.log(userId);

let customers: (number | string)[] = [1, "vaishnav"];
console.log(customers);